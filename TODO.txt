
    - support for renegotiation
    - support for SSL2 (low prio)
    - verify signatures (signed public key)
    - certificate validation and extraction to server profile
    - add a progress feedback, e.g. print a dot for each handshake
    - debug-logging: First log the incoming message type, then the parameters
    - implement CCM and CCM_8
    - HelloRetryRequest
    - more scan results
    - improved error handling
    - ProtocolData: try to minimize castings to/from bytes/bytearray, e.g. 
      provide a class with static methods only
